# ============================================================
# Alamat kontrak per jaringan untuk frontend (Bonus: Multiple Network + Hosting)
# Salin menjadi .env (lokal) atau set sebagai Environment Variables
# di dashboard Vercel/Netlify saat hosting.
# ============================================================

# Alamat kontrak di Hardhat Local (opsional — sudah ada default di kode).
VITE_LOCAL_CONTRACT=0x5FbDB2315678afecb367f032d93F642f64180aa3

# Alamat kontrak hasil `npm run deploy:sepolia`.
VITE_SEPOLIA_CONTRACT=
